const double bottomBarHeight = 60.0;
const double topBarHeight = 60.0;
const double navigationBarHeight = 60.0;

const double ABOUT_ME_CIRCLE_RADIUS = 160;

int currentIndex = 0;

setCurrentIndex(index) {
  currentIndex = index;
}

getCurrentIndex() {
  return currentIndex;
}
